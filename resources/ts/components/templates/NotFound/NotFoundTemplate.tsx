export const NotFoundTemplate = ({ children }) => {
    return (
        <main className="bg-[#111931] min-h-screen flex items-center justify-center">
            {children}
        </main>
    );
};
